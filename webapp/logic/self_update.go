package logic

import (
    "gosha/webapp/types"
)

func SelfUpdateFind(filter types.SelfUpdateFilter)  (result []types.SelfUpdate, totalRecords int, err error) {

	//SelfUpdate Find logic code

    return
}

func SelfUpdateCreate(filter types.SelfUpdateFilter)  (data types.SelfUpdate, err error) {
    
	//SelfUpdate Create logic code
    return
}
